Attempted repro for https://github.com/microsoft/playwright/issues/16424

1. Clone repo, change to directory
1. Install deps: `npm i`
1. Run `npx playwright test` repeatedly

Observe tests pass repeatedly.